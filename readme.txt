conda create -n mlops-env python=3.11 -y
conda activate mlops-env
conda install -r conda_requirements.txt
pip install -r pip_requirements.txt
download and install vc_redist.x64.exe then restart your pc